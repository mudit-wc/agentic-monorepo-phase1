{
  "name": "@agentic/ado-audit-hook",
  "version": "0.1.0",
  "private": true,
  "description": "Azure Function that posts a change-log comment on every Azure DevOps work item update (Service Hook consumer).",
  "main": "dist/src/functions/*.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "start": "func start",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~6.0.3"
  }
}
